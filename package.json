{
  "name": "squdy-platform",
  "version": "1.0.0",
  "description": "Squdy Campaign Platform",
  "scripts": {
    "start": "cd backend && node start.js",
    "dev": "cd backend && npm run dev",
    "install-backend": "cd backend && npm install"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}